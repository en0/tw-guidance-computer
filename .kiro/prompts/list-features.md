# List all features that are not yet implemented.

List only tracked features that are not yet fully implemented. Exclude future ideas, refactors, and
anything not promoted to a feature.

For each, use this format:

# <feature-id> - <feature-name> [<status>]

<one-sentence description>

Order by readiness: features that can be started now first, then those blocked on one dependency,
etc.
